import 

class Country {
    constructor(player) {
    this.name = countryname1;
    this.owner = player.playerID;
    }
}

export function startGame(player, globe, gameOptions){

    //Populate country object in database
    //Create countries and players for game state
    
    
    //Assign countries to players
    globe.owner
    globe.countries = ["Argentina", "Venezuela", "3"]
    let countriesShuffled = countries
        .map(value => ({value, sort:Math.random()}))
        .sort((a, b) => a.sort - b.sort)
        .map(({value}) => value)
    
    countryCount = length(countries)
    for (i = 0; i < countryCount; i++){
        const countryName1 = new Country(player)
    }

}

